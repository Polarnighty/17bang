@model SRV.ViewModel.EnityDto.CommentDto

@{ 
    string reply = Model.CommentId == null ? "clearfix comment" : "pull-right commentBy";
}
<div class="@reply">
    <div>
        <small class="text-muted"><span class="fa fa-calendar"></span><span style="margin:0px 5px;">@Model.CommentTime</span></small>
    </div>
    <div class="clearfix" style="margin:10px 0;">
        @Model.Content
    </div>
</div>

